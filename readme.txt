To build the library type:

make

from a command shell.
After build, you'll see one library in the lib folder:

cwmidi.dll

Then, you can build the example to test it.
Maybe the library and the example will be already compiled, but you can rebuild them if you wish.
 